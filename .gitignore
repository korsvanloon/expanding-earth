node_modules
dist
.DS_Store
*.local

# Generated by `npm run data` from public/textures/age-map.png.
# Reproducible, so not committed.
/public/data/*.bin
/public/data/*.json
/public/data/*.jpg
/public/data/*.png
/public/data/*.sha
# The coarse meshes the viewer's explorer solves on: generated, like the rest of
# public/data, and the patterns above are one level deep so this needs its own.
/public/data/preview/

# TypeScript incremental build state
*.tsbuildinfo
/.stage

# Reference figures and raw datasets gathered by the research pass (RESEARCH-FINDINGS.md):
# third-party material, kept local. research/README.md carries the manifest and fetch commands.
/research/data/
/research/figures/
